import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  button: {
    fontWeight: 700,
    fontStyle: "normal",
    fontSize: 16,
    color: "#ffffff",
    backgroundColor: "#acd373",
    borderColor: "#95c74d",
    borderRadius: 0,
    padding: "10px 32px",
  },
}));
